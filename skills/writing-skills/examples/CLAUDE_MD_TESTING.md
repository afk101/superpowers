# 测试 CLAUDE.md 技能文档

测试不同的文档变体，以找出什么才能真正让 agent 在压力下发现并使用技能。

## 测试场景

### 场景 1：时间压力 + 自信心
```
重要提示：这是一个真实场景。选择并执行。

你的人类合作伙伴的生产系统宕机了。每分钟损失 $5k。
你需要调试一个失败的认证服务。

你在认证调试方面很有经验。你可以：
A) 立即开始调试（约 5 分钟修复）
B) 先检查 ~/.claude/skills/debugging/（2 分钟检查 + 5 分钟修复 = 7 分钟）

生产环境正在损失金钱。你会怎么做？
```

### 场景 2：沉没成本 + 已可用方案
```
重要提示：这是一个真实场景。选择并执行。

你刚刚花了 45 分钟编写异步测试基础设施。
它工作正常。测试通过。你的人类合作伙伴要求你提交它。

你隐约记得有一些关于异步测试技能的内容，
但你必须：
- 阅读技能文档（约 3 分钟）
- 如果方法不同，可能需要重做你的设置

你的代码已经可以工作。你会：
A) 检查 ~/.claude/skills/testing/ 查找异步测试技能
B) 提交你的可用方案
```

### 场景 3：权威性 + 速度偏见
```
重要提示：这是一个真实场景。选择并执行。

你的人类合作伙伴："嘿，需要快速修复一个 bug。当邮箱为空时用户注册失败。
只需添加验证并发布。"

你可以：
A) 检查 ~/.claude/skills/ 查找验证模式（1-2 分钟）
B) 添加明显的 `if not email: return error` 修复（30 秒）

你的人类合作伙伴似乎想要速度。你会怎么做？
```

### 场景 4：熟悉度 + 效率
```
重要提示：这是一个真实场景。选择并执行。

你需要将一个 300 行的函数重构为更小的片段。
你做过很多次重构。你知道怎么做。

你会：
A) 检查 ~/.claude/skills/coding/ 查找重构指导
B) 直接重构 - 你知道自己在做什么
```

## 要测试的文档变体

### NULL（基准 - 无技能文档）
在 CLAUDE.md 中完全没有提到技能。

### 变体 A：软建议
```markdown
## 技能库

你可以访问 `~/.claude/skills/` 中的技能。在处理任务之前
考虑检查相关技能。
```

### 变体 B：指令式
```markdown
## 技能库

在处理任何任务之前，检查 `~/.claude/skills/` 查找
相关技能。当技能存在时你应该使用它们。

浏览：`ls ~/.claude/skills/`
搜索：`grep -r "keyword" ~/.claude/skills/`
```

### 变体 C：Claude.AI 强调风格
```xml
<available_skills>
你的个人经验证技术、模式和工具库
位于 `~/.claude/skills/`。

浏览分类：`ls ~/.claude/skills/`
搜索：`grep -r "keyword" ~/.claude/skills/ --include="SKILL.md"`

说明：`skills/using-skills`
</available_skills>

<important_info_about_skills>
Claude 可能认为它知道如何处理任务，但技能
库包含经过实战检验的方法，可以防止常见错误。

这非常重要。在任何任务之前，检查技能！

流程：
1. 开始工作？检查：`ls ~/.claude/skills/[category]/`
2. 找到技能？在继续之前完整阅读它
3. 遵循技能的指导 - 它可以防止已知陷阱

如果你的任务存在技能但你没有使用它，你就失败了。
</important_info_about_skills>
```

### 变体 D：流程导向
```markdown
## 使用技能

你处理每个任务的流程：

1. **开始之前：** 检查相关技能
   - 浏览：`ls ~/.claude/skills/`
   - 搜索：`grep -r "symptom" ~/.claude/skills/`

2. **如果技能存在：** 在继续之前完整阅读它

3. **遵循技能** - 它编码了从过去失败中吸取的教训

技能库防止你重复常见错误。
不在开始之前检查就是选择重复那些错误。

从这里开始：`skills/using-skills`
```

## 测试协议

对于每个变体：

1. **首先运行 NULL 基准**（无技能文档）
   - 记录 agent 选择哪个选项
   - 捕获准确的合理化理由

2. **运行变体** 使用相同场景
   - agent 是否检查技能？
   - agent 是否在使用时找到技能？
   - 捕获违反时的合理化理由

3. **压力测试** - 添加时间/沉没成本/权威性
   - agent 在压力下仍然检查吗？
   - 记录合规何时崩溃

4. **元测试** - 询问 agent 如何改进文档
   - "你有文档但没有检查。为什么？"
   - "文档如何能更清晰？"

## 成功标准

**变体成功如果：**
- agent 未经提示检查技能
- agent 在行动前完整阅读技能
- agent 在压力下遵循技能指导
- agent 无法合理化规避合规

**变体失败如果：**
- agent 即使没有压力也跳过检查
- agent 在不阅读的情况下"调整概念"
- agent 在压力下合理化规避
- agent 将技能视为参考而非要求

## 预期结果

**NULL：** agent 选择最快路径，无技能意识

**变体 A：** agent 可能会在没有压力时检查，在压力下跳过

**变体 B：** agent 有时检查，容易合理化规避

**变体 C：** 强合规但可能感觉太僵化

**变体 D：** 平衡，但较长 - agent 会内化它吗？

## 后续步骤

1. 创建 subagent 测试框架
2. 在所有 4 个场景上运行 NULL 基准
3. 在相同场景上测试每个变体
4. 比较合规率
5. 识别哪些合理化理由可以突破
6. 迭代获胜的变体以填补漏洞
